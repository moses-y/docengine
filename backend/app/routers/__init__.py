"""HTTP routers. Each module owns one resource and does nothing but parse
requests, call a service function, and shape the response — all business
logic and permission checks live in `app.services`.
"""
